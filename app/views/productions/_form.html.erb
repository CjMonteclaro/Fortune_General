<%= form_for(production) do |f| %>
  <% if production.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production.errors.count, "error") %> prohibited this production from being saved:</h2>

      <ul>
      <% production.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :intermediary %>
    <%= f.text_field :intermediary %>
  </div>

  <div class="field">
    <%= f.label :intm_no %>
    <%= f.number_field :intm_no %>
  </div>

  <div class="field">
    <%= f.label :branch %>
    <%= f.text_field :branch %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
